@model Web.Models.ModelView.SectionView

<div class="panel panel-primary">
    <div class="panel-heading">
        <h2 class="panel-title">Section Information</h2>
    </div>
    <div class="panel-body">
        @using (Ajax.BeginForm("Edit", "Section", new AjaxOptions { UpdateTargetId = "ajaxDetails", InsertionMode = InsertionMode.Replace, HttpMethod = "POST" }))
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(model => model.Section.SectionId)

            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        @Html.LabelFor(model => model.Section.SectionName)
                        @Html.EditorFor(model => model.Section.SectionName, new { htmlAttributes = new { @class = "form-control" } })
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label>Room</label>
                        @Html.DropDownListFor(model => model.Section.RoomId, Model.RoomOptions, new { @class = "form-control" })
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="panel panel-info">
                        <div class="panel-heading">
                            <h2 class="panel-title">Subject List</h2>
                        </div>
                        <div class="panel-body">
                            <div class="table-responsive">
                                <table class="table table-striped table-bordered table-condensed">
                                    <thead>
                                        <tr>
                                            <th>
                                                Select
                                            </th>
                                            <th>
                                                Subject
                                            </th>
                                            <th>
                                                Description
                                            </th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @Html.EditorFor(model => model.Subject)
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-offset-0 col-md-12">
                    <input type="submit" value="Save" class="btn btn-primary" /> |
                    @Ajax.ActionLink("Back", "Index", "Section", null, new AjaxOptions { UpdateTargetId = "ajaxDetails", InsertionMode = InsertionMode.Replace }, new { @class = "btn btn-default" })
                </div>
            </div>
        }
    </div>
</div>
@section Scripts{

    <script>
        $(".chkSelect").bind("change", function () {
            if (!$(this).prop("check")) {
                $("#chkSelectAll").prop("checked", false);
            }
        });
    </script>
}